#ifndef CSVLINEPOLULATE_H
#define CSVLINEPOLULATE_H
#include <vector>
#include <string>

using namespace std;

void csvline_populate(vector<string> &record, const string& line, char delimiter);

#endif

